/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Remote logos / images would be configured here when wiring real data sources.
  images: {
    remotePatterns: [],
  },
};

export default nextConfig;
